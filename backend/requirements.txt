fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
asyncpg==0.29.0
alembic==1.13.2
pydantic==2.9.2
pydantic-settings==2.5.2
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.9
redis==5.0.8
email-validator==2.2.0
slowapi==0.1.9
itsdangerous==2.2.0

# Экспорт ответов для исследователей. openpyxl нужен для XLSX-выгрузки,
# pandas — для подготовки прямоугольных таблиц и для pyreadstat (он
# принимает только DataFrame). pyreadstat пишет SPSS .sav с метками
# переменных и значений — это «золотой стандарт» в социальных науках,
# без него датасет в HSE никто всерьёз не возьмёт. Все три пакета
# поставляются готовыми wheel-ами на PyPI, компилятор в Docker не зовётся.
openpyxl==3.1.5
pandas==2.2.3
pyreadstat==1.2.7
